__pycache__/
*.pyc

# COCO images + raw Karpathy annotation file -- large, fully reproducible via
# `python scripts/prepare_data.py` (fixed seed, see README)
data/images/
data/dataset_coco.json
data/*.json

# trained weights -- large, regenerate via scripts/run_fl_simulation.py etc.
checkpoints/
checkpoints_smoke/
checkpoints_timing/

# raw run logs -- keep pipeline_validation.md and metrics json, drop noisy logs
logs/*.log
